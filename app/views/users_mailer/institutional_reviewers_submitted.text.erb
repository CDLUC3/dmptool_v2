<% if @vars[:body].nil? %>
	Hello <%= @vars[:user].full_name %>,

	The DMP "<%= @vars[:plan].name %>" has been submitted for institutional review.

	If you have questions pertaining to this action, please visit the Review DMPs page at <%= perform_review_plan_url(@vars[:plan].id) %>

	<%= render :partial => 'users_mailer/notification_boilerplate.text.erb' %>

<% else %>
	<%= @vars[:body] %>
<% end %>