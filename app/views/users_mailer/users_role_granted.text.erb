Hello <%= @vars[:user].full_name %>,

Your user account has had the following role(s) assigned to it.

<%= @vars[:granted_roles].join(", ") %>

You currently have the following roles.

<%= @vars[:user].roles.pluck(:name).join(", ") %>

If you have questions pertaining to this action, please contact the DMP Administrator at uc3@ucop.edu.

<%= render :partial => 'users_mailer/notification_boilerplate.text.erb' %>