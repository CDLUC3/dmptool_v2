.row-fluid
	%div.existing_template.span6
		%h5 Copy an Existing DMP Template
		%p Select an existing resource template below and edit the template information and details.
		= form_tag copy_existing_resource_template_path, method: 'get' do
			%table.table.table-condensed.span6
				%thead
					%tr
						%th
						%th Name
						%th Status
				%tbody
				- @resource_templates.each do |resource_template|
					%tr
						%td= radio_button_tag :resource_template, resource_template.id, checked: false
						%td= resource_template.name
						%td= resource_template.active ? "active" : "inactive"
			.actions.span6
				= submit_tag 'Template Information', class: 'btn btn-primary'

	%div.new_template.span6
		%h5 Create New DMP Template
		%p Input basic and optional information of the new resource template and edit the template details.
		%br
		=button_to 'Template Information', new_resource_template_path, :method => :get, class: 'btn btn-primary'