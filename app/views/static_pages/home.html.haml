%h4 Data Management Plan Tool
%br/
.container-fluid
  .row-fluid
    .span4
      %p Create, share, review, and publish Data Management Plans conforming to institution andfunder requirements.
      %br/
      = button_to 'Log In', login_path, class: 'btn btn-large btn-primary', method: :get
      = link_to 'Create a DMP Tool Account', new_user_path
    .span8
      = holder_tag '600x250', 'DMP tool image'

  .contain
    .row-fluid
      .span4
        = render 'data_management_plans'
      .span4
        = render 'how_to_use'
      .span4
        = render 'dmptool_news'
