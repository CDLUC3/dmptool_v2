%h1 Listing users

%table
  %tr
    %th Institution
    %th Email
    %th First name
    %th Last name
    %th Token
    %th Token expiration
    %th Prefs
    %th
    %th
    %th

  - @users.each do |user|
    %tr
      %td= user.institution_id
      %td= user.email
      %td= user.first_name
      %td= user.last_name
      %td= user.token
      %td= user.token_expiration
      %td= user.prefs
      %td= link_to 'Show', user
      %td= link_to 'Edit', edit_user_path(user)
      %td= link_to 'Destroy', user, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New User', new_user_path
